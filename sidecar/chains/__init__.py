"""Per-chain payment + discovery rails.

Step-0 scaffold of the multichain refactor (see MULTICHAIN_PLAN.md). The
``base`` module holds the target interfaces; concrete rails live in
``chains/ton/`` and (later) ``chains/solana/``.
"""
